<% if @session_user && @session_user.admin? %>
  <div class="row">
    <div class="col-sm-12">
      <center>
        <a href="<%= new_article_path %>" rel="nofollow">
          <button type="button" class="btn btn-info btn-lg">New Article</button>
        </a>
      </center>
    </div>
  </div>
<% end %>